delta = b ** 2 - 4 * a * c
    if delta > 0:
        return (-b - delta ** 0.5) / 2 * a, (-b + delta ** 0.5) / 2 * a
    elif delta == 0:
        return -b / 2 * a
    else:
        return None


print(zeroOfFunction(1, 2, 1))
